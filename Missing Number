class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        leng = len(nums)
        nums.sort()
        for i in range(0,leng+1):
            try:
                if (nums[i]==i):
                    pass
                else :
                    return i
            except IndexError:
                return i
